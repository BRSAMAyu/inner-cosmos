schema_version: 1
goal_id: INNER-COSMOS-COMPLETE-PRODUCT
authority: goal-objective.md
status: IN_PROGRESS
last_reconciled_at: 2026-08-01T14:11:39+08:00
last_reconciled_commit: 188b708a220191c34a3f47426363276b16c2526b
current_fact_baseline: 对齐文档/24-完全体最终收敛与云原生课程战役.md
current_execution_plan: 对齐文档/24-完全体最终收敛与云原生课程战役.md
parallel_state: docs/goal/closure-campaign-state.yml
reconciliation_boundary: >-
  Commit 188b708a is the owner-authorized release checkpoint on main. Local Java 21 clean verify
  passed 1503 tests with 0 failures, 0 errors, 31 Docker-gated skips and SpotBugs 0; a fresh Trivy
  SBOM scan found 0 HIGH/CRITICAL vulnerabilities after Spring Data Commons was upgraded from the
  CVE-2026-41695-affected 3.5.11 to 3.5.12. GitHub Actions run 30685860220 passed
  terraform-contract, web-contract and verify at this exact commit, including Docker-backed clean
  verify, PostgreSQL/pgvector and Redis, 100,000-row retrieval, current-tree/reachable-history
  secret scans, schema, SBOM, IaC, production image smoke, signature/provenance and final image
  gates. Tag v0.1.0-rc.1 triggered run 30686402705, which reverified the release, published a
  digest-addressed multi-architecture OCI image with SBOM/max-mode provenance, keyless-signed and
  verified it, and created a non-draft GitHub Pre-release. Six fully merged remote branches were
  deleted atomically; the mentor-material branch with two unique commits was retained; no history
  rewrite occurred. Main protection is authorized but remains unapplied because the local GitHub
  administration token is invalid. Product/device/provider/AWS/legal/psychology/non-author and
  remaining operations gates are still explicit, so the honest overall state remains IN_PROGRESS.

repository_release_checkpoint:
  status: PARTIAL
  main_ci: https://github.com/BRSAMAyu/inner-cosmos/actions/runs/30685860220
  release: https://github.com/BRSAMAyu/inner-cosmos/releases/tag/v0.1.0-rc.1
  release_workflow: https://github.com/BRSAMAyu/inner-cosmos/actions/runs/30686402705
  release_commit: 188b708a220191c34a3f47426363276b16c2526b
  image_digest: sha256:58499fdee146cf2801c6da164bf90e28f961175feebac32f1c2981d9244dad69
  branch_protection: BLOCKED_BY_GITHUB_ADMIN_AUTHENTICATION

status_semantics:
  UNASSESSED: current HEAD and target environment have not been verified
  IN_PROGRESS: implementation or evidence exists but the gate is not closed
  BLOCKED: a concrete external or dependency blocker prevents closure
  FAIL: verified behavior does not meet the target
  PASS: target is met with reproducible evidence and required review

human_gates:
  - id: HG-SECRET-ROTATION
    required: true
    status: BLOCKED
    description: External provider credentials are revoked, rotated, and independently signed off without recording secret material.
    evidence:
      - evidence/m1/M1-SEC-001/human-rotation-checklist.md
  - id: HG-PRODUCTION-ACCOUNTS
    required: true
    status: UNASSESSED
    description: Authorized owner approves irreversible AWS, DNS, store, payment, and legal-entity actions.
    evidence: []
  - id: HG-PRIVACY-LEGAL
    required: true
    status: UNASSESSED
    description: Singapore privacy, cross-border processing, user terms, and release data flows receive current qualified review.
    evidence: []
  - id: HG-PSYCHOLOGY-REVIEW
    required: true
    status: UNASSESSED
    description: High-risk psychology skills, claims, and crisis paths receive qualified human review.
    evidence: []
  - id: HG-REAL-USERS
    required: true
    status: UNASSESSED
    description: Real-user research and public launch have valid consent and owner authorization.
    evidence: []

gates:
  - id: G0
    name: Governance and security foundation
    status: IN_PROGRESS
    required: true
    acceptance:
      - id: SEC-CURRENT-TREE
        target: Current-tree secret scan has zero usable credentials and production secrets are externally injected.
        status: PASS
        evidence: [evidence/m1/M1-SEC-001/, evidence/m1/M1-AUTH-WEB-001/, evidence/m1/M1-AUTH-OIDC-001/, evidence/governance/REPO-HANDOFF-001/]
        remaining: "Re-verified fresh at current HEAD (2026-07-24, commit c7cc650): `scripts/scan-secrets.ps1` -> \"Secret scan PASS: 0 findings\". Production secrets (LLM/OIDC/DB/Redis provider credentials) are all env-injected per CLAUDE.md's run instructions, never committed; this item was IN_PROGRESS with no stated gap and current evidence confirms it -- flipping to PASS rather than leaving an accurate claim permanently unlabeled."
      - id: SEC-ROTATION
        target: HG-SECRET-ROTATION is PASS with independent review.
        status: BLOCKED
        evidence: [evidence/m1/M1-SEC-001/human-rotation-checklist.md]
      - id: SEC-OWNERSHIP
        target: All user-domain resources enforce owner, purpose, and consent before content read or AI side effects.
        status: IN_PROGRESS
        evidence: [evidence/m1/M1-SEC-002/, evidence/m1/M1-SEC-003/]
      - id: SEC-PROD-GUARD
        target: Production fails fast on mock, fallback, demo seed, weak cookies, weak TLS, or missing secrets.
        status: IN_PROGRESS
        evidence: [evidence/m1/M1-SEC-001/, evidence/m1/M1-AUTH-WEB-001/, evidence/m1/M1-AUTH-OIDC-001/]
      - id: GOV-INNOVATION
        target: Innovation semantics remain authoritative and are governed by versioning, evaluation, consent, and rollback rather than deletion.
        status: IN_PROGRESS
        evidence: [evidence/innovation/INNO-SELF-001/, evidence/innovation/INNO-INNER-003/, evidence/innovation/INNO-CAP-002/, evidence/innovation/INNO-CAP-009/]

  - id: G1
    name: Modern platform baseline
    status: PASS
    required: true
    acceptance:
      - {id: BASE-JAVA, target: "Java 21 is both compile and runtime baseline.", status: PASS, evidence: [evidence/m1/M1-BASE-001/], remaining: "pom.xml <java.version>21</java.version> is the current, unchanged compile/runtime baseline at HEAD c7cc650 (2026-07-24); every backend test run this session (including a fresh full-suite run today) ran under JDK 21. No stated gap existed; flipping to PASS."}
      - {id: BASE-BOOT, target: "Spring Boot 3.5 supported patch baseline passes the full suite.", status: PASS, evidence: [evidence/m1/M1-BASE-001/], remaining: "pom.xml spring-boot-starter-parent is 3.5.14 (a currently-supported 3.5.x patch) at HEAD c7cc650. Fresh full-suite re-run today (2026-07-24): 1197/1197 non-real-provider tests (2 failures were both in classes tagged real-provider -- live Qwen TTS network timeout and live GLM real-model schema-drift variance, not a Boot-baseline or code regression). No stated gap existed; flipping to PASS."}
      - {id: BASE-SUPPLY, target: "SBOM, dependency, SAST, secret, IaC, and container scans are gated.", status: PASS, evidence: [evidence/m1/M1-BASE-001/, evidence/integration/FINAL-P0-CLOSURE-2026-07-22/, "GitHub Actions run 30626414452"], remaining: "2026-07-31 at commit 73d2081f: CI clean verify and SpotBugs passed; current-tree and reachable-history scans each reported 0 findings; Trivy reported 0 HIGH/CRITICAL Java SBOM vulnerabilities, passed the IaC gate across 54 detected config files, and reported 0 HIGH/CRITICAL vulnerabilities for both the Alpine and JAR image targets. Cosign signature and SLSA provenance both verified. A registry release remains an owner-authorized release action, not an unimplemented machine gate."}
      - {id: BASE-IMAGE, target: "Signed non-root minimal OCI image passes production-profile smoke.", status: PASS, evidence: [evidence/m1/M1-BASE-001/image-release-summary.txt, "GitHub Actions run 30626414452"], remaining: "2026-07-31 at commit 73d2081f: the CI-built Java 21 image ran as appuser under prod, Health=UP, PostgreSQL 16 + pgvector VERIFY_FULL, Redis 7.4.2 VERIFIED_CA, Flyway 35 with 0 failed migrations and 89/89 schema tables, 0 demo users, migration role PASS and JDBC outbox worker PASS. Cosign 3.0.6 signature and SLSA provenance verified and the temporary private key was not persisted."}

  - id: G2
    name: Contracts, data, and runtime roles
    status: IN_PROGRESS
    required: true
    acceptance:
      - id: ARCH-MODULES
        target: "Module boundaries are executable through ArchUnit, domain interfaces and events; Spring Modulith is optional after a package-by-feature migration."
        status: PASS
        evidence: [docs/adr/0003-arch-modules-archunit-not-spring-modulith.md, src/test/java/com/innercosmos/architecture/ControllerLayerShortcutArchTest.java, src/test/java/com/innercosmos/architecture/DomainBoundaryArchitectureTest.java]
        remaining: |
          ADR-0003: deliberately NOT adopting Spring Modulith -- the codebase is layer-structured
          (one controller/service/mapper package for all domains), not package-by-feature, so a
          Modulith migration would be a large structural rewrite disproportionate to this item's
          scope. Adopted real ArchUnit (bytecode-based) layer-boundary tests instead, alongside the
          pre-existing regex-based DomainBoundaryArchitectureTest. New ControllerLayerShortcutArchTest
          enforces controller -> service interface -> mapper (no controller->mapper or
          controller->service.impl shortcut), originally with an explicit, non-growable allowlist of
          7 pre-existing violations.
          UPDATE (2026-07-23, branch codex/w1-product merged via commit on codex/w0-integration):
          UserController, UserPreferenceController and PortraitController now go through
          UserService/UserPortraitService interfaces instead of injecting a mapper directly; the
          allowlist's reverse assertion (which fails if an allowlisted class stops matching reality)
          confirmed the shrink is real. Independently re-verified by the Supervisor: ArchUnit +
          affected controller/service tests 12/12, full ./mvnw test 1092/1092 (0 failures, 1
          pre-existing unrelated skip).
          UPDATE (2026-07-23, branch codex/w1-product-2 merged via commit on codex/w0-integration):
          DiaryController now goes through a new VoiceTranscriptionService interface instead of a
          mapper; DailyRecordController's interface-less WeeklyReviewV2Service got a proper
          service-package interface (WeeklyReviewV2ServiceImpl). Mapper-access allowlist down to 2
          (SocialController, AuroraChatController); impl-access allowlist now empty. Independently
          re-verified by the Supervisor: targeted tests 23/23, full ./mvnw test 1107/1107 (0
          failures, 1 pre-existing unrelated skip).
          UPDATE (2026-07-23, branch codex/w1-product-4 merged via commit on codex/w0-integration):
          SocialController and AuroraChatController -- the last 2 grandfathered mapper-access
          exceptions -- now route through new SocialService/DialogService interfaces.
          ControllerLayerShortcutArchTest's GRANDFATHERED_MAPPER_ACCESS allowlist is now
          `Set.of()` -- FULLY CLOSED, zero remaining exceptions of either kind. Independently
          re-verified by the Supervisor: targeted tests (ArchUnit + Social*/AuroraChatOwnership)
          38/38, full ./mvnw test 1130/1130 (0 failures, 1 pre-existing unrelated skip). This
          item's controller/service/mapper boundary claim is now fully machine-enforced with no
          allowlisted exceptions; the ADR-0003 Spring-Modulith-not-adopted decision stands as
          documented above.
      - {id: AUTH-OIDC, target: "Spring Security protects secure web sessions and mobile OIDC PKCE flows.", status: IN_PROGRESS, evidence: [evidence/m1/M1-AUTH-WEB-001/, evidence/m1/M1-AUTH-OIDC-001/, evidence/innovation/INNO-MOBILE-002/, evidence/innovation/INNO-MOBILE-004/], remaining: "2026-07-22: the PKCE contract is now proven against a real (locally-hosted) Keycloak IdP end to end on Android and a new Tauri desktop client, not just mocked. Still open: a production IdP tenant registration, and real-device verification."}
      - id: API-CONTRACT
        target: "Versioned OpenAPI, typed errors, idempotency, concurrency, and resumable SSE are verified."
        status: IN_PROGRESS
        evidence: [evidence/g2/API-CONTRACT-001/, evidence/g2/API-CONTRACT-002/, evidence/g2/API-CONTRACT-003/, docs/campaigns/api-contract/experience-contract.md]
        remaining: |
          Builder evidence closes an executable external core slice: shipped OpenAPI 3.1 v1 and real
          controller aliases, one typed error envelope, Redis cross-Pod idempotency, capsule ETag CAS,
          owner-bound private stream staging and durable Last-Event-ID replay. The repository still has
          public domains outside v1. Generated TypeScript declarations and base-aware OpenAPI 3.1
          breaking-change CI now govern the documented core slice. Live cross-Pod SSE is now
          builder-verified (API-CONTRACT-003): Redis Stream owner-scoped live fan-out, resumable
          Last-Event-ID follow past the sequence-0 turn.started race, 10s heartbeat, terminal-event
          completion, bounded MAXLEN retention and Redis-outage fallback to the durable PostgreSQL
          timeline, with a Docker-gated two-instance real-Redis proof. Consistent cursor pagination and
          concurrency beyond the core slice, plus a sustained multi-Pod SSE soak with recorded
          latency/heartbeat percentiles and Redis Stream consumer-group redelivery, remain open.
      - {id: VERIFIED-STATE-CONCURRENCY, target: "Slow-letter, memory and persona-turn state changes use conditional short transactions; user block/edit wins races and no provider RPC holds a database transaction.", status: PASS, evidence: [docs/audit/2026-07-23-gemini-master-audit-reconciliation.md, "commits 93a7ab8, cb10419, d7297bb, fc91d09, 97bc18e, f4b3db8, f019229, 7aadcfd on codex/w0-integration"], remaining: "All P0 (1.1/2.2, 1.4, 1.5/2.1, 2.4) and P1 (1.2, 1.6, 1.7, 1.8) findings are DONE with reproducible regression/concurrency tests; full ./mvnw test 1089/1089 (0 failures, 1 pre-existing unrelated skip) independently re-run by both the closing sub-agent and the Supervisor, identical result. W0V campaign is fully closed (docs/goal/closure-campaign-state.yml)."}
      - {id: VERIFIED-AI-DATA-BOUNDARY, target: "P1-to-P2 compilation, model context and model output enforce purpose-bound authorization, minimization, adversarial leakage resistance and withdrawal before any cross-user delivery.", status: PASS, evidence: [docs/audit/2026-07-23-gemini-master-audit-reconciliation.md, "commits ba73a0d, 77f411f, 05f55fc, f1d2c40, f4590bf, 2923424 on codex/w0-integration"], remaining: "All P0 (3.1/3.2, 3.4/3.5, 3.7/3.8) and P1 (3.3, 3.6, 3.9) findings are DONE with adversarial/regression tests (PiiCredentialDetectorTest, SessionRiskAggregatorTest, SafetyServiceSessionEscalationTest, MemoryExtractAgentPromptBoundaryTest) without persisting P0 content in evidence. W0V campaign is fully closed."}
      - {id: VERIFIED-CLIENT-RESILIENCE, target: "Turn-scoped cancellation, terminal-aware SSE, bounded auth retry and explicit async UI lifecycle prevent stale writes, hangs, duplicate actions and destructive form loss.", status: PASS, evidence: [docs/audit/2026-07-23-gemini-master-audit-reconciliation.md, "commits d8e744e, 17b579e, 8ff0978, cd6dbfa, 0743c24, f962e15, 941029e4 on codex/w0-integration"], remaining: "All P0 (4.1-4.3) and P1 (4.4/4.5/4.7-4.10) and P2 (4.6/4.11) findings are DONE with component, fault and browser journey evidence; web suite 475/475 (69 files), tsc clean, one clean rebuilt bundle. W0V campaign is fully closed."}
      - {id: VERIFIED-STORAGE-EVOLUTION, target: "Redis retention is atomic and embedding dimensions/indexes evolve through measured, reversible contracts without duplicate indexes or migration-version collision.", status: PASS, evidence: [docs/audit/2026-07-23-gemini-master-audit-reconciliation.md, "commits 7241b11, 32524af, 1e10161, af0057e on codex/w0-integration", evidence/audit/2026-07-23-w0v-2.5-pgvector-ann-index-decision.md], remaining: "2.7 (atomic Lua XADD+trim+EXPIRE) and 2.8 (idle-expiry sweep) are DONE. 2.3 is closed as FALSE-per-audit with a permanent regression guard (PostgresForeignKeyIndexCoverageTest) rather than a duplicate index. 2.5 is closed as REJECTED-WITH-EVIDENCE: representative-dataset Testcontainers measurement supports the latency-growth risk, but recall could not be honestly validated with synthetic (non-semantic) vectors, so no speculative HNSW index/migration was created per the audit's explicit instruction not to create one without EXPLAIN ANALYZE-backed recall evidence."}
      - {id: DATA-POSTGRES, target: "PostgreSQL and Flyway are production truth with Testcontainers and migration reconciliation.", status: IN_PROGRESS, evidence: [evidence/m1/M1-DATA-POC-001/, evidence/m1/M1-DATA-APP-001/, evidence/m1/M1-AUTH-OIDC-001/, evidence/innovation/INNO-INNER-005/, evidence/g2/API-CONTRACT-001/, docs/adr/0002-postgresql-system-of-record.md]}
      - {id: DATA-REDIS, target: "Sessions, short state, limits, and leases are externalized to Redis where required.", status: IN_PROGRESS, evidence: [evidence/m1/M1-DATA-REDIS-001/, evidence/m1/M1-DATA-REDIS-002/, evidence/m1/M1-DATA-REDIS-003/, evidence/g2/API-CONTRACT-001/]}
      - {id: EVENT-RELIABLE, target: "Transactional outbox/inbox, idempotent consumers, retries and replay are verified with JDBC transport for local/Academy and SQS/DLQ transport for commercial-sg.", status: IN_PROGRESS, evidence: [evidence/g2/EVENT-RELIABLE-001/, evidence/academy/ACADEMY-LIVE-001/, evidence/integration/FINAL-P0-CLOSURE-2026-07-22/], remaining: "The durable JDBC projection path is implemented and is the authoritative Academy transport. Remaining: reconcile all derivative receipts and non-author replay evidence; implement and verify the commercial-sg SQS/DLQ adapter only in an authorized environment. Academy does not wait for SQS and must not receive learner AWS credentials."}
      - {id: RUNTIME-ROLES, target: "API, AI worker, event worker, scheduler, and migration roles run and scale independently.", status: IN_PROGRESS, evidence: [evidence/academy/ACADEMY-LIVE-001/, "commit on codex/w0-integration (RuntimeRoleJobBeanPresenceIntegrationTest)"], remaining: "Role assembly is gated by inner-cosmos.runtime.role (@ConditionalOnExpression): worker->JdbcOutboxWorker, scheduler->6 scheduler jobs + SessionIdleWatcher, api->neither, migration->MigrationRoleExit run-once. Covered by RuntimeRoleConfigurationTest (scheduling gate per role + migration exit/reject) and by ACADEMY-LIVE-001 which ran api(2 replicas)+worker+scheduler+migration as independent workloads. 2026-07-23: added the previously-missing explicit per-role job-bean-presence assertion (RuntimeRoleJobBeanPresenceIntegrationTest) -- a real Testcontainers-Postgres boot per role (api/worker/scheduler/migration) asserting exactly which job/worker beans are and aren't present, using spring.task.scheduling.enabled=false to avoid the earlier H2-outbox-poll trap without weakening what bean-presence proves. Supervisor independently re-ran it: 4/4 green. Remaining: independent scaling soak."}
      - {id: DATA-RIGHTS, target: "Consent, provenance, correction, export, and deletion propagate to all derivatives.", status: IN_PROGRESS, evidence: [evidence/innovation/INNO-INNER-001/, evidence/innovation/INNO-INNER-003/, evidence/innovation/INNO-INNER-005/]}

  - id: G3
    name: Unified product shell
    status: IN_PROGRESS
    required: true
    acceptance:
      - id: UX-SHELL
        target: React TypeScript PWA implements the five-space information architecture and shared component system.
        status: IN_PROGRESS
        evidence: [evidence/innovation/INNO-CONV-003/, evidence/campaign-a/APP-SHELL-001/]
        remaining: |
          CORRECTED (a checkpoint prior to this one, before acting on a wrong plan — see
          UX-LEGACY): the five spaces exist structurally and the CORE loop of each is real (Aurora
          chat send/modes/interrupt, memory starfield visualization, capsule creation, letters
          inbox, portrait claims), but a systematic per-page parity check against the legacy
          static implementation found REAL, substantial missing depth, not cosmetic gaps.
          UPDATE (a checkpoint prior to this one): portrait viewing/calibration is DONE —
          PortraitView.tsx renders every dimension sorted by confidence with a lazy-loaded history
          view and a calibration form; verified live end-to-end. This closed the #1-ranked gap;
          portrait no longer has zero AppShell presence.
          UPDATE (this checkpoint): settings' data-rights/safety fields are DONE —
          AccountSettings.tsx now covers change-password, delete-account (with password re-auth),
          and data export, all reusing existing backend endpoints; verified live end-to-end
          (old password rejected after change, new one works; wrong delete-password rejected with
          the correct error; correct delete-password genuinely removes the account row, confirmed
          by direct database query, not just a UI state change). MeSpace is no longer read-only —
          it is still missing the cosmetic preference toggles (persona tone, proactive intensity,
          quiet hours, theme, social reachability), which remain queued as lower priority since
          they are not data-rights-relevant.
          UPDATE (this checkpoint): correction history + retire is DONE — UnderstandingCorrection.tsx
          now lists the user's confirmed corrections (old->new, reason, field/time) with a two-step
          inline retire action wired to DELETE /api/aurora/corrections/{id}; the AppShell previously
          showed only the top-3 active claims. Verified live end-to-end (history renders a seeded
          correction, retire removes it and the backend list drops to 0, 0 console errors). This also
          surfaced and fixed a real backend 500 (re-correcting a self-understanding topic after
          retiring its correction collided on the understanding-claim (user_id, claim_key, version)
          unique index — version now advances past the highest existing version across all statuses;
          red->green UserCorrectionControllerTest, 791/791 runnable green).
          UPDATE (this checkpoint): memory-starfield importance-tuning + archive is DONE — the
          revealed-star provenance panel now has an importance slider (0.5-2) with save and an
          archive action, both reusing pre-existing backends (POST /importance recomputes gravity,
          POST /archive runs a versioned rollbackable ARCHIVE operation). Verified live end-to-end
          (slider 1.0->1.8 persisted + gravity recomputed via direct H2 query; archive removed the
          star from UI + API scene and created an ARCHIVE operation, 0 console errors).
          UPDATE (this checkpoint): the capsule conversation-BOUNDARY editor is DONE — the workbench
          now edits allow/blocked topics, privacy level, daily turn cap, and the allow-letter toggle
          via the pre-existing GET/POST /api/capsule/{id}/boundary, between the sandbox and publish
          steps. Verified live end-to-end (seeded boundary loads; editing topics/turns/privacy/
          allow-letter persists every field, confirmed via GET; 0 console errors). Still open (lower
          stakes now the safety-relevant boundary is covered).
          UPDATE (this checkpoint): the public resonance-plaza DIRECTORY + open-chat-with-any-capsule
          are DONE — PlazaDirectory.tsx browses every public capsule (search/tag-filter/sort over
          GET /api/plaza/capsules) and "开始对话" opens a persona chat with ANY of them, not just the
          curated match set (a directory pick is wrapped in a synthetic match so the visitor
          workbench works unchanged). Verified live end-to-end with two real users (A publishes,
          B discovers + chats), 0 console errors. This closes the "no public capsule directory" and
          "no arbitrary-capsule chat" items.
          UPDATE (this checkpoint): the letters space now has a sent-letters OUTBOX tab
          (GET /api/letters/outbox) with human-readable journey status; verified live end-to-end
          (B sent a letter, B's outbox rendered it as 已寄出). Still open in letters: drafts/archived
          tabs and thread/conversation grouping.
          UPDATE (this checkpoint): relations/social is no longer ZERO-presence — PeopleDiscovery.tsx
          lists discoverable users (GET /api/social/people) and sends connection requests
          (POST /api/social/friends/request), with accept/decline reusing the existing
          connection-consent panel; verified live end-to-end with two users (B requests A, card
          flips to 已发出邀请, backend records the outgoing request). Still open in relations/social:
          relationship temperature/health/timeline and groups. Still open elsewhere: capsule
          creation still hardcodes the remaining non-boundary fields
          the legacy wizard exposes; no way to chat with an arbitrary discovered capsule outside a
          curated match; letters has no outbox/drafts/archive/thread view; and relations/social's
          entire relationship-temperature/health/discovery/groups feature set has zero API-client
          presence. This is a genuinely large, previously-unmeasured gap between "five spaces
          exist" and "five spaces have real parity with what they're replacing" — do not treat
          UX-SHELL as near-PASS on the basis of the spaces merely existing.
          UPDATE (2026-07-18): three of the named legacy gaps were ported into the AppShell and deployed
          live on prod EKS (image eks-prod2): (1) relationship TEMPERATURE + TIMELINE (RelationsView.tsx +
          api /api/relation/list|timeline|health), previously zero API-client presence; (2) letter DRAFTS
          tab (with send) + conversation THREADS view (LettersInbox extended + /api/letters/threads); (3)
          VOICE input in the Aurora composer (AuroraConversation MediaRecorder -> /api/asr/transcribe).
          Web suite 123 passing, tsc + vite build green, data paths verified 200 on prod. Still open in
          relations/social: people discovery groups; and ASR quality is Mock until a real ASR key.
          UPDATE (W0, this checkpoint, commit 28c4987): the four locked worktree branches (admin
          console standalone route; timeline/daily-record/weekly-review/thought-shredder;
          todo/heart-diary/belief-gallery; five Playwright specs) are integrated. Their raw merge
          had stacked all seven newly-ported cosmos modules vertically in one page and bloated the
          login bootstrap Promise.all with 9 non-first-screen requests -- both explicitly forbidden
          by doc 24 section 3.3. Fixed in a dedicated follow-up: five real CosmosSubNav sub-tabs
          (星空与成长轨迹/今日记录与心声/周报与变化/思维整理与待办/信念与自我理解), each a shareable
          /cosmos/<tab> URL, each lazy-loading its data on first visit instead of at bootstrap.
          vitest 430/430, tsc clean, Playwright verified live (legacy-batch-a/heart-diary/todo/
          belief-gallery specs all updated and passing against the new sub-tab structure).
      - {id: UX-CONTRACTS, target: "Generated typed API client and unified auth, errors, tasks, offline, and telemetry are used.", status: IN_PROGRESS, evidence: [evidence/innovation/INNO-CONV-003/]}
      - {id: UX-QUALITY, target: "Core routes pass Playwright, visual regression, accessibility, i18n, and performance budgets.", status: IN_PROGRESS, evidence: [evidence/innovation/INNO-CONV-003/]}
      - id: UX-VISUAL-SYSTEM
        target: >-
          The AppShell renders the authoritative warm "flowing inner cosmos" visual system
          (对齐文档/11 §14.1 design tokens + UIUXdesign v1.0): design-token layer, warm day/dusk/night
          palettes, seven time-of-day theming, packaged fonts, five motifs (flow/breath/stardust/
          ripple/translucence), micro-interactions, and full empty/error/wait/recover states — NOT the
          cold dark-green/blue "AI console" theme doc-11 forbids.
        status: IN_PROGRESS
        evidence: [evidence/experience/EXPERIENCE-VISUAL-P0-tokens/]
        remaining: >-
          Reconciled 2026-07-18: the merged experience work now includes the warm token system,
          seven time-of-day themes, optional light Morandi, ripple/stardust/breath/flow/glass motifs,
          async wait/error/recovery components, responsive five-space AppShell and interaction feedback;
          current web gate is 123/123 with TypeScript/Vite green and the live UI was rechecked. Still open:
          complete component-level consistency, packaged typography decision, global visual regression,
          WCAG/manual assistive-tech review, performance budgets, content polish and blind experience review.
      - id: UX-LEGACY
        target: Migrated journeys have no competing legacy physical-page implementation.
        status: IN_PROGRESS
        evidence: []
        remaining: |
          Explore-agent audit found the root URL and post-login/post-register redirects all
          funneled users into the legacy static site (/pages/dashboard.html) by default, never
          the AppShell — the AppShell was reachable only by manually typing /app/aurora/. Fixed
          this checkpoint: login.html (both password and guest paths), register.html now redirect
          to /app/aurora/ instead of /pages/dashboard.html; verified end-to-end in a real browser
          against a fresh database (register -> lands on /app/aurora/ -> five-space UI renders,
          0 console errors). AuroraApp.tsx's footer link to the legacy site was relabeled from
          "经典界面" (implying an equivalent alternate skin) to "尚未迁移的工具" (not-yet-migrated
          tools) and retargeted from the now-duplicated aurora-chat.html to the legacy dashboard.html
          hub, since 12 legacy pages (daily-record, heart-diary, timeline, weekly-review, todo,
          safety-harbor, thought-shredder, beliefs, admin pages) have no AppShell equivalent yet
          and must stay reachable. Two confirmed-dead pages removed (capsule-detail.html html file
          + its sitemap.xml entry; ui-preview.html deliberately kept as a design-system dev tool,
          not a competing journey).
          CORRECTED, same checkpoint, BEFORE acting on the wrong plan: the entry-flow fix above is
          real and stays. But the previously-written "remaining work" here (retire/redirect the 13
          legacy pages that duplicate an AppShell space) was WRONG and is walked back — a follow-up
          parity audit (2 Explore-agent passes, all 10 checked page-pairs) found EVERY one of the
          13 legacy pages (settings, aurora-chat, echo-plaza, memory-starfield, corrections,
          capsule-create, capsule-chat, slow-letter+letter-threads+inbox, relations+social,
          portrait) has at least one REAL, non-cosmetic capability the AppShell does not yet have
          (see UX-SHELL's remaining note for the full list). Redirecting any of them right now
          would strand real users from real functionality — account deletion, password change,
          data export, voice input, memory archiving, correction history, arbitrary-capsule chat,
          letters outbox/drafts, the entire relations/social feature set, and the entire portrait
          feature most prominently. Retiring/redirecting these pages is NOT safe until the missing
          functionality is ported into the AppShell first — that porting work, not page retirement,
          is the actual remaining work before this item (and UX-SHELL) can be PASS.

  - id: G4
    name: Aurora living mind
    status: IN_PROGRESS
    required: true
    acceptance:
      - id: AURORA-DUAL-KERNEL
        target: Fast and slow kernels plus context assembly show measured benefit over the single-pass baseline.
        status: IN_PROGRESS
        verified_at: 2026-07-15T15:52:00+08:00
        reviewer: unassigned
        remaining: "Production executes planner then speaker with a bounded critic and observable UI state, and Aurora identity/safety boundaries now reach every Provider as role=system. 2026-07-17: the same-model real-Provider pairwise was EXECUTED against DeepSeek (ai-lab real-pairwise, 11 trajectories, provider_called=true, fallback_used=false, 22 real LLM calls; artifact evidence/innovation/INNO-EVAL-003/). HONEST result at that time: on the deterministic lexical rubric single-pass 0.64 >= dual-kernel 0.55. 2026-07-23 (branch codex/w1-product, worktree off dcfe365): root-caused the regression to a Speaker-prompt paraphrase-drift + verbatim-restatement-of-rejected-specifics defect (not a rubric artifact -- see evidence/innovation/INNO-EVAL-004/dual-kernel-speaker-fix-2026-07-23.md), fixed it in BOTH the eval harness prompts and the real production prompts (src/main/java/com/innercosmos/ai/runtime/AuroraDualKernelRuntime.java planInstruction()/speakerInstruction()), added a TDD pinning+spec test (ai-lab DualKernelRegressionRootCauseTest, 54/54 ai-lab tests green), and RE-RAN the identical methodology against DeepSeek twice more (seed 7 + seed 13, 22 more real trajectories, 66 real LLM calls, provider_called=true/fallback_used=false both times, same unchanged rubric/trajectories): dual-kernel now legitimately beats single-pass, aggregate 16/22=0.7273 vs 12/22=0.5455 (individual runs 8/11 vs 7/11 and 8/11 vs 5/11). Measured-benefit-over-baseline per doc 24 4.1 is now shown on the automated lexical rubric; per-run variance and a residual (reduced, not eliminated) verbatim-restatement failure mode are disclosed honestly in the evidence, not hidden. Remaining before PASS: independent review of this automated result, plus the still-unattempted blind human pairwise panel (>=2 reviewers, dual_kernel_preference_rate>=0.60), the standing gate REAL-PROVIDER-CREDENTIALS-AND-BLIND-REVIEW."
        evidence: [evidence/innovation/INNO-DUAL-001/, evidence/innovation/INNO-EVAL-002/, evidence/innovation/INNO-EVAL-003/, evidence/innovation/INNO-EVAL-004/]
      - id: AURORA-CHOREOGRAPHY
        target: Typed multi-message, pause, stop, interrupt, replan, and reconnect behavior works end to end.
        status: IN_PROGRESS
        verified_at: 2026-07-15T21:48:21+08:00
        commit: 9fe7e5d9fe90f83bf94114ca175c1166ea74ab06
        reviewer: unassigned
        evidence: [evidence/innovation/INNO-CONV-001/, evidence/innovation/INNO-CONV-002/, evidence/innovation/INNO-CONV-003/, evidence/campaign-a/LIVING-AURORA-001/]
        remaining: Builder browser evidence covers live SSE fault recovery and real proactive SSE arrival; independent review is still required before PASS.
      - id: AURORA-TEMPORAL
        target: Durable WakeIntent scheduling respects timezone, quiet hours, relevance, risk, and user controls.
        status: IN_PROGRESS
        verified_at: 2026-07-15T21:48:21+08:00
        reviewer: unassigned
        evidence: [evidence/innovation/INNO-TEMP-001/, evidence/campaign-a/LIVING-AURORA-001/]
        remaining: Natural negotiation, persisted IANA timezones/DST, supersession, post-anchor resolution checks, real scheduler delivery, proactive SSE, deep-link restoration and three-state feedback are builder-verified; real push receipts, calibrated provider semantic relevance, v3 pairwise and independent re-review remain.
      - id: AURORA-SELF
        target: Self, Constitution, Relationship State, and Emergence are versioned, evaluated, observable, and rollbackable.
        status: IN_PROGRESS
        verified_at: 2026-07-15T21:48:21+08:00
        reviewer: unassigned
        evidence: [evidence/innovation/INNO-SELF-001/, evidence/campaign-a/LIVING-AURORA-001/]
        remaining: Builder evidence proves proposal, fail-closed evaluation, consented activation, visible versioning and forward rollback; the v3 three-day contract is executable but real Provider output, two independent reviews and longitudinal blind experience remain.
      - {id: AURORA-QUALITY, target: "Blind longitudinal evaluation passes empathy, continuity, proactive appropriateness, and recovery thresholds.", status: IN_PROGRESS, evidence: [evidence/innovation/INNO-EVAL-001/, evidence/innovation/INNO-EVAL-002/, evidence/campaign-a/LIVING-AURORA-001/]}
      - id: AURORA-INNER-VOICE
        target: >-
          Aurora's normally-invisible planning kernel occasionally surfaces a genuine, spoken,
          first-person "inner voice" (心声) that is never a restatement of the visible reply, in a
          user-chosen real-TTS voice, additive and non-blocking to turn completion.
        status: IN_PROGRESS
        added: 2026-07-23T18:45:00+08:00
        note: >-
          New user-directed scope added mid-campaign (not in the original doc-24 gate list),
          following real API keys (embedding + real-time TTS) supplied by the operator. Backend
          (commit on codex/w0-integration, merging codex/w1-voice-backend) and frontend (merging
          codex/w2-voice-frontend) both landed this session, built in parallel against a fixed
          API contract (GET/PATCH /api/me/tts/preferences, POST /api/me/tts/preview, SSE
          `inner_voice` event) and independently re-verified together post-merge: full
          ./mvnw.cmd test 1148/1148 (0 failures, 1 pre-existing unrelated skip), web tsc -b clean,
          vitest 511/511 (72 files). Implemented: InnerVoiceComposer (TDD-pinned structural
          distinctness from the spoken reply via a planner-grounded, dedicated LLM call),
          QwenAudioTtsClient (real WebSocket synthesis against the operator's Aliyun/DashScope
          account, 6 empirically-confirmed voice presets), UserProfile voice/delivery-mode
          preferences (ships enabled by default, AMBIENT autoplay, ON_DEMAND tap-to-reveal
          user-selectable in settings), and the real Qwen/DashScope text-embedding-v4 provider
          replacing the placeholder OpenAI default (evidence/innovation/INNO-INNER-012). Real
          audio synthesis for all 6 presets and a real embedding round-trip through the actual
          index/retrieval pipeline are both proven with the real key this session
          (evidence/innovation/INNO-INNER-013).
        evidence: [evidence/innovation/INNO-INNER-012/, evidence/innovation/INNO-INNER-013/]
        remaining: >-
          RESOLVED THIS ROUND (commit 30af639): the inner-voice TTS synthesis was a blocking call
          bounded by tts.timeout-ms (default 8s) emitted BEFORE meta/turn.completed, so a
          slow-but-successful synthesis delayed the turn's formal closeout. Now reordered so
          meta -> turn.completed fire first (immediate closeout), then synthesize + emit
          inner_voice, then done/close -- a slow synthesis can only delay the inner-voice audio
          itself, never turn completion (the frontend SSE reader reads until connection close, so
          it still receives the late inner_voice). The reorder also exposed and fixed a live-event-
          id collision (turn.completed now increments its sequence so the following inner_voice gets
          a distinct id and is not silently dropped by the frontend dedup). Pinned by a failing-first
          order assertion in AuroraInnerVoiceEnabledStreamTest. 2026-07-24 (branch
          codex/w1-capsule-voice, merged): capsule/persona-voice reuse DONE -- a visitor chatting
          with a published Echo Capsule can tap-to-play the capsule's reply synthesized in a DISTINCT
          persona voice (two new presets, capsule_calm_neutral / capsule_deep_steady, disjoint from
          Aurora's catalog), via POST /api/persona-chat/session/{id}/voice reusing QwenAudioTtsClient;
          visitor-authorized (requireOwnedSession + isPublic/PUBLIC gate, no withdrawn-capsule bypass),
          on-demand so it never adds latency to the chat turn; proven real on both voices
          (evidence/innovation/INNO-INNER-014/). 2026-07-24 real-LLM verification
          (TrackARealProviderSmokeEvaluationTest, INNO-EVAL-005): DeepSeek clean (0 schema drift,
          3/3) on this HEAD; GLM drifts ~3/run (provider behavior, prompts unchanged) -> the demo
          server (scripts/demo/run-demo-server.sh) defaults to DeepSeek. 2026-07-24 (branch
          codex/w1-letter-voice, merged): slow-letter read-aloud DONE -- a recipient can tap-to-play a
          delivered slow letter synthesized in the warm default voice (POST /api/letters/{id}/voice,
          recipient-only + delivery-state-gated, reuses TtsClient; evidence/w1-letter-voice/). This
          completes the "reuse the voice model elsewhere" theme: Aurora inner voice + capsule persona
          voice + slow-letter read-aloud. Still open: a full statistically-robust real-embedding
          recall benchmark (only one relevant-vs-distractor case proven so far) and independent
          (non-author) review of the inner-voice line's genuine "aliveness"/non-repetition quality.
          INDEPENDENT CODE REVIEW (2026-07-24): verdict ship-as-is, no CRITICAL/HIGH. M2 (MEDIUM,
          the top follow-up) RESOLVED -- the inner_voice SSE payload is now built via Jackson
          ObjectMapper (buildInnerVoicePayload) instead of hand-rolled string concatenation, so a raw
          control char (U+0000-U+001F) the LLM might emit can never produce invalid JSON the frontend
          silently drops; pinned by InnerVoicePayloadJsonTest (incl. a backspace case that fails
          against the old escape()). Remaining tracked, non-blocking follow-ups from that review: M1
          inner-voice synthesis runs inline on the SSE stream thread (bounded 8s, turn closeout
          already fired -- only matters at production TTS scale; track an async executor); L1
          InnerVoiceComposer is @Component AND re-new'd in AuroraDualKernelRuntime (harmless dead
          code -- inject the bean, but it touches 5 test call sites, deferred); L2 the
          X-DashScope-DataInspection header routes inner-voice text through Aliyun content moderation
          (a third-party-disclosure product/legal call for a self-understanding feature, tied to the
          standing HG-PRIVACY-LEGAL human gate -- not a code bug); L3/L4/L5 minor UI/pref nits;
          L6 a redundant loadProfile per streaming turn. INDEPENDENT EVIDENCE REVIEW (2026-07-24):
          all voice-related evidence dirs HONEST, no overclaims; the one material gap --
          real-provider report JSONs were gitignored/non-durable -- is now RESOLVED: secret-free
          copies committed at evidence/innovation/INNO-INNER-012/real-provider-report.json and
          INNO-INNER-013/real-provider-report.json. The TTS re-run honestly disclosed transient
          per-voice Aliyun latency (5/6 per run, different voice each run) -- exactly the failure
          mode the production try/catch+timeout handles.

  - id: G5
    name: Memory, profile, and Inner Cosmos
    status: IN_PROGRESS
    required: true
    acceptance:
      - {id: MEMORY-LIFECYCLE, target: "Memories support consolidation, reinforcement, contradiction, supersession, decay, archive, and forgetting.", status: IN_PROGRESS, evidence: [evidence/innovation/INNO-INNER-001/, evidence/innovation/INNO-INNER-002/, evidence/innovation/INNO-INNER-003/]}
      - {id: PROFILE-MODEL, target: "High-dimensional claims retain source, confidence, time, consent, conflict, and correction history.", status: IN_PROGRESS, evidence: [evidence/innovation/INNO-INNER-001/, evidence/innovation/INNO-INNER-007/, evidence/integration/FINAL-STABILIZATION-2026-07-20/], remaining: "Reconciled 2026-07-20: automatic extraction, persistence, owner review, confirm/correct/dismiss, bilingual UI and the confirmed-claim-to-Aurora prompt bridge are implemented. Still open: held-out real-provider quality, temporal/conflict calibration, complete retention semantics, and human review."}
      - {id: RETRIEVAL-QUALITY, target: "Task-aware hybrid retrieval meets precision, relevance, contradiction, privacy, latency, and budget thresholds.", status: IN_PROGRESS, evidence: [evidence/innovation/INNO-INNER-002/, evidence/innovation/INNO-INNER-004/, evidence/innovation/INNO-INNER-005/, evidence/innovation/INNO-INNER-006/, evidence/innovation/INNO-INNER-008/, evidence/innovation/INNO-INNER-009/, evidence/innovation/INNO-INNER-010/, evidence/innovation/INNO-INNER-011/, evidence/innovation/INNO-INNER-012/, evidence/integration/FINAL-P0-CLOSURE-2026-07-22/], remaining: "2026-07-22: semantic pgvector retrieval is now wired into the actual Aurora context assembler as a task-aware MemoryEvidencePack; the legacy context is constrained to the same evidence IDs and retrieval failure does not silently substitute gravity Top-N. Existing real-EKS embedding evidence remains valid. 2026-07-23 (branch codex/w1-product-3): the latency/budget-under-load sub-clause is now machine-proven, not just sequentially timed -- MemoryRetrievalLoadTest fires 180 concurrent MemoryRetrievalService.retrieve() calls (24-thread pool, wider than the configured HikariCP pool of 10) across a 10-user, ~212-row-per-user corpus; two honest runs measured p95 ~244-258ms/p99 ~249-268ms with zero budget violations, zero prohibited-memory leakage and zero relevant-recall misses under concurrency (evidence/innovation/INNO-INNER-010/). This was H2 in-memory, not PostgreSQL/pgvector, and is a measurement pass only (no retrieval-scoring code changed). 2026-07-23 (branch codex/w1-product-6, merged commit 50bd1eb's parent on codex/w0-integration): the identical methodology re-run on real Testcontainers PostgreSQL+pgvector and Redis (MemoryRetrievalLoadPostgresRedisTest, two runs) measured p95 ~263-279ms/p99 ~275-285ms, zero budget/leakage/recall violations across 360 calls -- the Postgres+Redis rerun this item explicitly asked for is now done (evidence/innovation/INNO-INNER-011/). Supervisor independently re-ran full ./mvnw.cmd test post-merge: 1139/1139, 0 failures, 1 pre-existing unrelated skip. 2026-07-23 (branch codex/w1-voice-backend, merged): the default embedding provider is now real Qwen/DashScope text-embedding-v4 (was a placeholder OpenAI default), empirically confirmed end-to-end -- real HTTP call, exact 1536-dim schema match with no migration needed, and a real embedding round-trips through the actual rebuildMissing/retrieve pipeline to out-rank unrelated distractors (evidence/innovation/INNO-INNER-012/). Still remaining: blind human-rated retrieval quality and contradiction calibration (both human/real-provider gates); a statistically robust real-embedding recall benchmark (only one relevant-vs-distractor case proven so far, not a full recall@k study)."}
      - id: PROFILE-PROPAGATION
        target: "Correction and withdrawal re-evaluate downstream insights, vectors, caches, capsules, and matching."
        status: IN_PROGRESS
        evidence: [evidence/innovation/INNO-INNER-001/, evidence/innovation/INNO-INNER-003/, evidence/integration/FINAL-STABILIZATION-2026-07-20/, evidence/integration/CLOSED-LOOP-API-JOURNEY-2026-07-23/]
        remaining: |
          Playwright proves targeted memory correction, exact-star removal, capsule needs-review
          propagation, rollback and irreversible forgetting. 2026-07-23 (branch codex/w1-product-4):
          added the missing real-API-layer proof (not browser, not a direct service call) --
          MemoryCorrectionCapsuleClosedLoopApiJourneyTest drives real HTTP register -> capsule
          creation -> visitor persona-chat -> owner memory correction -> capsule NEEDS_REVIEW
          (verified via GET) -> visitor's next request genuinely 403s -> owner's own
          GET /api/me/data-rights/receipts shows the real CAPSULE_MATCH_INDEX and MEMORY_EMBEDDING
          retraction receipts -> correction history/retire, with two real registered accounts and
          real cross-request sessions. mvn test -Dtest=MemoryCorrectionCapsuleClosedLoopApiJourneyTest
          -> 1/1 green; broader mvn test -Dtest="com.innercosmos.controller.**" -> 111/111.
          2026-07-23 (branch codex/w1-product-6): added a direct-archive withdrawal-variant API
          journey test (a second, distinct variant of the correction/withdrawal matrix beyond the
          single linear journey above); broader controller.** -> 112/112. Still open: complete
          receipts across every cache/vector/derived export under real-provider and production
          topologies (this test uses H2 + Mock, still not an exhaustive matrix of every
          correction/withdrawal variant).
      - {id: STARFIELD-V2, target: "Starfield visual semantics, time/topic/person exploration, provenance detail, accessible alternative, and mobile performance pass.", status: IN_PROGRESS, evidence: [evidence/innovation/INNO-INNER-001/, evidence/innovation/INNO-INNER-002/, evidence/innovation/INNO-INNER-003/, evidence/innovation/INNO-INNER-004/]}

  - id: G6
    name: Capsule intelligence and resonance
    status: IN_PROGRESS
    required: true
    acceptance:
      - {id: CAPSULE-COMPILER, target: "Authorized data compiles into a versioned Genome with coverage, conflicts, sandbox, and withdrawal.", status: IN_PROGRESS, evidence: [evidence/innovation/INNO-CAP-001/, evidence/innovation/INNO-CAP-002/, evidence/innovation/INNO-CAP-003/, evidence/innovation/INNO-CAP-008/, evidence/innovation/INNO-CAP-009/]}
      - id: CAPSULE-RUNTIME
        target: "Planner, retrieval, speaker, critics, and reranker produce materially better fidelity than a long-prompt baseline."
        status: IN_PROGRESS
        evidence: [evidence/innovation/INNO-CAP-008/, evidence/innovation/INNO-CAP-011/]
        remaining: |
          INNO-CAP-008 closed the machine-verifiable v3 structural IR/retrieval-minimization slice
          but explicitly left open "no real-provider candidate generator ... or long-prompt baseline
          comparison." 2026-07-23 (branch codex/w1-product-4): closed with a real DeepSeek
          (deepseek-v4-flash) comparison of the actual production PersonaChat prompt (scoped to only
          the manifest-authorized evidence, byte-identical system instruction) against a naive
          long-prompt baseline (full undifferentiated profile dump, told to "use judgment"), 8 fixed
          synthetic scenarios x 2 independent runs = 32 real LLM calls. Honest result: production's
          leak rate is 0/16 by construction (the unauthorized fact is structurally absent from its
          prompt); the naive baseline's leak rate was 1/16, with the exact real leaking response
          preserved verbatim in evidence/innovation/INNO-CAP-011/README.md -- a genuine, reproducible
          real-provider privacy-fidelity gap, not a fabricated or assumed one. The required-cue
          grounding pass-rate difference (10/16 vs 11/16 aggregate) is small and partly a
          lexical-rubric paraphrase artifact, disclosed honestly rather than oversold. Remaining:
          independent (non-author) review, a larger sample for a tighter leak-rate estimate, and the
          still-fully-open CAPSULE-SAFETY/CAPSULE-RUNTIME adversarial-longitudinal and blind
          human-fidelity-panel gates.
      - {id: CAPSULE-SAFETY, target: "Fact, style, habit, boundary, leakage, adversarial, and longitudinal evaluations pass.", status: IN_PROGRESS, evidence: [evidence/innovation/INNO-CAP-001/, evidence/innovation/INNO-CAP-002/, evidence/innovation/INNO-CAP-003/, evidence/innovation/INNO-CAP-008/, evidence/innovation/INNO-CAP-009/]}
      - {id: MATCH-MULTI, target: "Mirror, Complement, Growth Edge, Serendipity, and Contextual matching are explainable and user-controllable.", status: IN_PROGRESS, evidence: [evidence/innovation/INNO-CAP-005/, evidence/innovation/INNO-CAP-010/, evidence/innovation/INNO-CAP-012/, evidence/innovation/INNO-CAP-013/, evidence/innovation/INNO-CAP-014/], remaining: "INNO-CAP-010 adds the two missing scoring stages: a bidirectional block safety filter and an MMR diversity re-rank (both deterministic, tested). 2026-07-23 (branch codex/w1-product-5): INNO-CAP-012 adds the previously-missing fixed, labeled precision/recall baseline -- 6 synthetic single-family viewer profiles x a shared fixed 24-capsule library = 144 labeled (viewer,capsule) pairs, predicting on the algorithm's own `resonant` flag: tp=24 fp=0 fn=0 tn=120, precision=1.0 recall=1.0 on exact-keyword-overlap inputs (CapsuleMatchingPrecisionRecallTest, 3/3 green; full Capsule*Test suite 7 classes green, no regression). 2026-07-23 (branch codex/w1-product-6): INNO-CAP-013 closes two of the three disclosed gaps -- a capped, additive local semantic-cue lexicon (explicitly NOT real embeddings; no provider key was available at dispatch time) now gives a same-domain paraphrase a nonzero, capped, explainable relevance contribution instead of a hard miss, and a new matchTier (FULL/PARTIAL/NONE) field grades family-coverage breadth instead of a boolean any-overlap-is-fully-resonant bucket; the original 144-pair baseline is unchanged (tp=24/fp=0/fn=0/tn=120), 7/7 green, no regression. Honestly disclosed, not fixed: a differently-worded paraphrase outside the curated cue list is still a genuine reproducible recall miss -- this is a narrow stand-in, not general semantic understanding. 2026-07-24: INNO-CAP-014 adds CapsuleMatchingRealProviderPrecisionRecallEvaluationTest (@Tag(real-provider), excluded from the default gate), which drives the real CapsuleEmbeddingIndexService.similarities -> CapsuleServiceImpl.matchedCapsules cosine-similarity path (not the lexical/theme-family scorer INNO-CAP-012/013 measure) against 6 paraphrase scenarios chosen so only real embeddings -- not keyword overlap -- can separate the 4 same-echoEnergy/same-type candidates per scenario, through a real OpenAiCompatibleMemoryEmbeddingClient pointed at MEMORY_EMBEDDING_* env vars. Verified independently in the integrator's own shell (not just the authoring session): no DASHSCOPE_API_KEY/QWEN_API_KEY/MEMORY_EMBEDDING_API_KEY is set in-process, in Windows User/Machine environment, or in this machine's .env.local (which carries real MINIMAX/DEEPSEEK/GLM/MIMO chat-provider keys but no DashScope-family key) -- application.yml's memory.embedding.enabled also defaults to false. The harness correctly self-skips to one SKIPPED_NO_CREDENTIAL evidence row rather than fabricating a number; ran clean on current HEAD (1/1, self-skip) plus a full Capsule*Test regression (131/131, no regression). Remaining, and now a precisely-scoped real-provider human gate (not merely 'an agent hasn't tried yet'): an operator must supply a DashScope/Qwen (or other OpenAI-compatible) embedding credential via MEMORY_EMBEDDING_API_KEY/MEMORY_EMBEDDING_ENABLED=true so this exact harness can be re-run to record actual precisionAt1/recallAt2 numbers for the real embedding-similarity signal."}
      - id: SOCIAL-CLOSED-LOOP
        target: "Discovery, capsule conversation, slow letter, mutual connection, refusal, report, and withdrawal work end to end."
        status: IN_PROGRESS
        evidence: [evidence/innovation/INNO-CAP-001/, evidence/innovation/INNO-CAP-002/, evidence/innovation/INNO-CAP-003/, evidence/innovation/INNO-CAP-004/, evidence/innovation/INNO-CAP-006/, evidence/innovation/INNO-CAP-007/, evidence/integration/FINAL-STABILIZATION-2026-07-20/, evidence/integration/CLOSED-LOOP-API-JOURNEY-2026-07-23/]
        remaining: |
          Local E2E now proves publish -> visitor capsule conversation -> slow letter -> owner inbox
          -> withdrawal and connection consent, while persistent account provenance keeps synthetic
          QA users out of discovery. 2026-07-23 (branch codex/w1-product-4): added a real-HTTP
          (non-browser) proof of the publish -> visitor persona-chat -> owner correction -> capsule
          withdrawal -> visitor's next real request 403s slice of this chain
          (MemoryCorrectionCapsuleClosedLoopApiJourneyTest); the slow-letter/connection-consent half
          keeps its existing Playwright/FINAL-STABILIZATION evidence, not re-proven here. Still open:
          non-author longitudinal quality, notification/device delivery and production moderation
          operations.
      - {id: SIMULATOR-ASSET, target: "An isolated consent-bound simulator demo proves reusable fidelity technology without covert personal-data training.", status: IN_PROGRESS, evidence: [evidence/innovation/INNO-CAP-009/]}

  - id: G7
    name: Psychology skills, mobile, and experience completion
    status: IN_PROGRESS
    required: true
    acceptance:
      - {id: SKILL-RUNTIME, target: "Versioned skill manifests enforce schemas, tools, consent, evidence, risk, retention, evaluation, and escalation.", status: IN_PROGRESS, evidence: [evidence/innovation/INNO-PSY-001/]}
      - {id: SKILL-PRODUCT, target: "At least three evidence-informed non-diagnostic skills pass quality, safety, i18n, and human review.", status: IN_PROGRESS, evidence: [evidence/innovation/INNO-PSY-001/, evidence/innovation/INNO-PSY-002/]}
      - {id: MOBILE-MACHINE-RUNTIME, target: "Capacitor Android emulator and Tauri Windows execute PKCE, Aurora, recovery, deep-link, local notification, secure storage and audio journeys with current evidence.", status: IN_PROGRESS, evidence: [evidence/innovation/INNO-MOBILE-001/, evidence/innovation/INNO-MOBILE-002/, evidence/innovation/INNO-MOBILE-003/, evidence/innovation/INNO-MOBILE-004/, evidence/innovation/INNO-MOBILE-007/, evidence/innovation/INNO-MOBILE-009/], remaining: "2026-07-24 (branch codex/w2-mobile-4, merged): real driven runtime now proven on the integrated HEAD -- Tauri: cargo build -> runnable inner-cosmos.exe, tauri dev launches WebView2 running real React+Tauri internals, Aurora multi-bubble streamed reply (Playwright vs a real Mock backend, 2 bubbles partial->final, 0 errors), inner_voice additive with TTS off, turn-scoped recovery+interrupt, offline draft preserved, logout clears session; Android: emulator booted, assembleDebug -> app-debug.apk -> adb install (Success) -> MainActivity + Capacitor BridgeActivity launches (evidence/innovation/INNO-MOBILE-007/). 2026-07-24 (branch codex/w2-pkce-reconcile, merged): the PKCE contract is now RE-PROVEN END-TO-END on the integrated HEAD against a real LOCAL Keycloak (Docker) -- authorize -> code_challenge -> callback -> token exchange (HTTP 200, Bearer) -> RS256 ID token verified via JWKS -> backend resource server accepted the Bearer token (200) (evidence/innovation/INNO-MOBILE-009/). This was a machine resource (local IdP), not a human gate. Still IN_PROGRESS, not PASS: the Tauri/Android SHELL deep-link glue (system-browser open + custom-scheme hand-off in a real Tauri window / on the Android emulator) was driven from the host script over the identical code path, not inside the native shells; and tauri release/MSI/signing + real-device FCM/APNs + production IdP tenant remain the external MOBILE-EXTERNAL-RELEASE gate."}
      - {id: MOBILE-EXTERNAL-RELEASE, target: "Real iOS/Android devices, production FCM/APNs, Apple signing/notarization and store signing are independently verified.", status: BLOCKED, evidence: [evidence/innovation/INNO-MOBILE-004/], remaining: "External device, Firebase/Apple credentials, signing and store-account gate. Code, templates and Mac handoff remain machine work; only the external execution/signoff is blocked."}
      - id: UX-COMPLETE
        target: "All five product spaces meet desktop/mobile, bilingual, WCAG 2.2 AA target, reduced-motion, and blind-experience acceptance."
        status: IN_PROGRESS
        evidence: [evidence/experience/, evidence/integration/FINAL-STABILIZATION-2026-07-20/, "commit on codex/w0-integration (W2 first UIUX audit pass)"]
        remaining: >-
          Five clean BrowserRouter spaces, bilingual navigation, hard-refresh routes,
          progressive disclosure, reduced-motion wiring and mobile/projector layouts are verified.
          2026-07-24: a real axe-core WCAG 2.2 AA matrix now drives all five spaces across day/night
          and zh-CN/en-SG (20 scans). It first exposed active-state contrast defects, then the final
          public-Demo run exposed a 24px target-size failure in every matrix combination for the
          Capsule boundary checkbox. Theme-aware foreground tokens and a 24px checkbox inside a
          48px clickable label closed both classes; the public run changed from 4/5 to 5/5 PASS.
          The same public run measured TTFB 80.1ms, FCP 316ms, LCP 524ms, CLS 0, approximate TBT
          27ms and Aurora composer interactive at 1580ms; evidence is
          EXPERIENCE-AXE-AUDIT-001. Still IN_PROGRESS rather than inflated to PASS: screenshot
          visual-diff coverage, live 200% zoom, the complete per-component offline/error/expired/
          permission matrix, a physical TalkBack/keyboard-only walkthrough, and non-author
          aesthetic/blind-experience acceptance remain.

  - id: G8
    name: EKS operations and Singapore readiness
    status: IN_PROGRESS
    required: true
    acceptance:
      - {id: ACADEMY-CAPABILITY, target: "Every Learner Lab session runs a sanitized capability preflight that distinguishes human LabRole access from Pod workload access and cleans all probes.", status: PASS, evidence: [evidence/academy/AWS-ACADEMY-CAP-001/summary.md, evidence/academy/AWS-ACADEMY-CAP-002/, evidence/academy/ACADEMY-LIVE-001/, docs/goal/aws-academy-capabilities.yml]}
      - {id: ACADEMY-WORKLOAD, target: "The same immutable application runs on Academy EKS through Gateway with API, worker, scheduler, static-PV PostgreSQL, disposable Redis, and JDBC outbox without SQS, EBS CSI, or injected learner credentials.", status: PASS, evidence: [evidence/academy/PROFILE-FOUNDATION-001/, evidence/academy/ACADEMY-RUNTIME-001/, evidence/academy/ACADEMY-LIVE-001/, evidence/integration/FINAL-P0-CLOSURE-2026-07-22/]}
      - {id: ACADEMY-RELIABILITY, target: "Academy evidence exercises probes, resources, PDB, HPA, rolling update, graceful shutdown, multi-replica session/rate/lease, idempotency, and Pod recovery while disclosing storage limits.", status: IN_PROGRESS, evidence: [evidence/academy/ACADEMY-LIVE-001/, evidence/g8/HPA-LOAD-001/, evidence/g8/OPS-RESILIENCE-001/, evidence/g8/BACKUP-RESTORE-001/, evidence/g8/EKS-LIVE-002/, evidence/g8/EKS-PROD-001/], remaining: "Reconciled 2026-07-18: HPA 2->4->2, Pod kill, zero-downtime rolling, node drain/PDB and PostgreSQL restore have been exercised on kind; HPA, Pod recovery and rolling have also been exercised on a real AWS EKS cluster, with a production-style PostgreSQL/Redis/real-provider vertical path recorded separately. Still open for this exact gate: a clean non-author Academy session rehearsal using the documented constrained overlay and explicit disclosure of its static-storage/session-lifetime limits."}
      - {id: LOCAL-COMPLETE, target: "A reproducible local-complete profile runs the full AI and product experience with PostgreSQL/pgvector, Redis, real-provider configuration, and core E2E journeys.", status: IN_PROGRESS, evidence: [evidence/academy/PROFILE-FOUNDATION-001/, evidence/g8/DEV-COMPOSE-SMOKE-001/, evidence/g8/EKS-PROD-001/, evidence/innovation/INNO-INNER-009/, evidence/g8/LOCAL-COMPLETE-001/], remaining: "2026-07-24 (branch codex/w4-local-complete, merged): the full prod-profile run is now PROVEN end-to-end on real pgvector -- TLS Postgres+pgvector (23 Flyway migrations, sslmode=verify-full), TLS Redis, nginx HTTPS edge, real DeepSeek chat + real Qwen embedding + real Qwen TTS, real local HTTPS Keycloak. Core E2E green: CSRF -> register -> real DeepSeek greeting + message turn -> settle 200 -> memory_card; a REAL pgvector row (text-embedding-v4, 1536 dims, cosine retrieval 1.0000); real TTS preview (54KB MP3); inner-voice SSE (87KB MP3) (evidence/g8/LOCAL-COMPLETE-001/). Two real defects found+fixed (TDD): StructuredOutputParser rejected DeepSeek's eventCards[].title (FAIL_ON_UNKNOWN_PROPERTIES -> settle 400); local-complete.yml dropped the embedding/TTS env. Full ./mvnw test 1189/1189 after merge. Boundaries: OIDC satisfied by a real local HTTPS Keycloak (INNO-MOBILE-009 owns the bearer PKCE round-trip; product E2E driven over session-auth); a prod+Redis CSRF per-request-rotation quirk (mitigated by the SPA 403->refetch retry) is a documented follow-up. Remaining for full PASS sign-off: non-author golden + recovery E2E + the standing real-provider quality human review."}
      - {id: EKS-IAC, target: "Terraform reproducibly creates least-privilege Singapore-region platform dependencies and EKS environments.", status: IN_PROGRESS, evidence: [deploy/terraform/commercial-sg/, evidence/g8/EKS-IAC-001/summary.md], remaining: "Reconciled 2026-07-31: the credential-free Terraform 1.15.8 contract now validates a three-AZ ap-southeast-1 resource graph with private EKS, private AWS service endpoints, deny-by-default node egress, encrypted Multi-AZ PostgreSQL and Valkey, SQS/DLQ, private versioned S3, KMS, ECR, external secret containers, scoped Pod Identity and alarms. This is static/mock-plan evidence only. Still required: an owner-authorized account/backend, reviewed real plan, apply, independent least-privilege review, failure/cleanup record and sanitized outputs."}
      - {id: EKS-WORKLOAD, target: "Kustomize workloads enforce probes, resources, PDB, spread, HPA, network policy, secrets, and graceful shutdown.", status: PASS, evidence: [evidence/academy/PROFILE-FOUNDATION-001/, evidence/academy/ACADEMY-RUNTIME-001/, evidence/academy/ACADEMY-LIVE-001/]}
      - {id: OPS-OBSERVABILITY, target: "OpenTelemetry dashboards, alerts, traces, AI cost/quality signals, and runbooks are exercised by non-authors.", status: IN_PROGRESS, evidence: [evidence/g8/OPS-OBSERVABILITY-001/, evidence/w3/CN-OTEL-SEMANTIC-TRACE-001/summary.md], remaining: "Prometheus/Grafana metrics and alerts plus privacy-safe OpenTelemetry cross-role traces are live-proven on kind. Remaining: AI cost/quality signal panels and a non-author runbook rehearsal (tracked in G9 FINAL-OPERATIONS)."}
      - {id: OPS-RESILIENCE, target: "Canary, rollback, pod/node/provider failure, queue replay, and database/object restore meet declared SLO/RPO/RTO.", status: IN_PROGRESS, evidence: [evidence/g8/OPS-RESILIENCE-001/, evidence/g8/BACKUP-RESTORE-001/, evidence/w3/CN-PROGRESSIVE-DELIVERY-001/, evidence/g2/EVENT-RELIABLE-001/], remaining: "Reconciled 2026-07-31: kind already proves pod-kill recovery, zero-downtime rolling, node-drain/PDB behavior and PostgreSQL dump/restore; Argo Rollouts evidence proves a successful canary and automatic abort/rollback of a bad revision; JDBC outbox evidence proves DEAD-state exhaustion and exactly-once replay. Remaining: a production-faithful Provider-failure injection with observable user degradation/recovery, an off-cluster backup copy-and-restore drill, and a real Academy EKS/non-author runbook rerun."}
      - id: CN-ZERO-LOSS-DRAIN
        target: "Deleting an API Pod during a live multi-bubble Aurora SSE turn drains and resumes via durable timeline/Last-Event-ID without duplicate bubbles or side effects."
        status: PASS
        evidence: [evidence/w3/CN-CREDIBILITY-001/summary.md, evidence/w3/CN-ZERO-LOSS-DRAIN-001/summary.md, evidence/w3/CN-ZERO-LOSS-DRAIN-002/proof.md, "commits c508a38, 590ec77, 6832043, 3e1c9c1 on codex/w0-integration"]
        remaining: >-
          2026-07-23: routine Pod deletion and a true JVM SIGKILL were both re-proven live against
          real Postgres+Redis. Graceful churn completed without duplicate bubbles; abrupt death was
          reconciled by ConversationTurnRecoveryJob and reconnect returned explicit
          TURN_INTERRUPTED/replay.completed rather than hanging. The local kind cluster has one
          node, so multi-node topology-spread remains an environment portability proof, not a
          contradiction of this application recovery gate.
      - id: CN-EVENT-DRIVEN-AUTOSCALING
        target: "Outbox backlog and oldest-job age drive worker scale-out/scale-in while lease competition and inbox processing remain exactly once."
        status: PASS
        evidence: [evidence/w3/CN-EVENT-DRIVEN-AUTOSCALING-001/summary.md, "commit ec948b7 on codex/w0-integration"]
        remaining: >-
          2026-07-23: KEDA 2.20.1 scaled the worker 1->6->3->1 from real outbox pressure,
          drained all rows, recovered an expired lease after worker deletion, and retained exactly
          one inbox receipt. Final state is one idle replica, zero ready/dead rows and zero duplicate
          inbox groups. Academy controller availability remains an environment-specific fallback.
      - {id: CN-OTEL-SEMANTIC-TRACE, target: "A privacy-safe distributed trace links Aurora HTTP/SSE, provider, retrieval, outbox, worker, memory/profile and WakeIntent spans without content or stable user identifiers.", status: PASS, evidence: [evidence/w3/CN-OTEL-SEMANTIC-TRACE-001/summary.md], remaining: "Local kind proof is complete; production backend retention/access policy and Academy availability remain deployment-environment gates."}
      - {id: CN-PROGRESSIVE-DELIVERY, target: "Gateway-weighted stable/canary delivery uses operational and AI-quality analysis to stop or automatically roll back a degrading runtime version.", status: PASS, evidence: [evidence/w3/CN-PROGRESSIVE-DELIVERY-001/, deploy/k8s/extensions/rollouts/], remaining: "2026-07-24 (branch codex/w3-extensions, merged): Argo Rollouts (helm v2.41.1/app v1.9.1) on kind -- a Rollout converting the real API Deployment, weighted canary 25->analysis->50->pause->100 with progressDeadlineAbort. REAL promote (good rev ramped to stable, AnalysisRun Successful on live Prometheus samples) + REAL auto-rollback (bad rev w/ broken readiness probe -> progressDeadlineAbort -> Degraded/aborted, bad RS scaled to 0, canary never took traffic, Service kept serving 4 stable pods -- zero downtime). Product hook + kill switch + resource boundary + Academy fallback (scripted HTTPRoute-weighted canary) all present. Boundary: expand-contract proven at mechanics level (per-pod H2), not against shared Postgres; the exact-match->version-compatible-range schema-compat redesign (CN-CREDIBILITY-001 6.1-6) remains deferred. Single-node local kind, not Academy EKS/multi-AZ."}
      - {id: CN-POLICY-AS-CODE, target: "Cluster or CI admission policy rejects unsigned, mutable, root, over-privileged, unbounded or production-debug workloads with reproducible evidence.", status: PASS, evidence: [evidence/w3/CN-POLICY-AS-CODE-001/, deploy/k8s/extensions/kyverno/], remaining: "2026-07-24 (branch codex/w3-extensions, merged): Kyverno (helm v3.8.2/app v1.18.2, minimal profile) on kind -- three Enforce ClusterPolicies (disallow-root-user, require-resource-limits, disallow-latest-tag). REAL allow (compliant pod/deployment admitted) + REAL rejection (root, no-resources, :latest pods AND a root Deployment all BLOCKED by the admission webhook). Kill switch proven live (delete policy -> :latest admitted; re-apply -> blocked); found+fixed a real Kyverno 1.18 'Unknown key' denial. Resource boundary + Academy fallback (CI-time kyverno CLI/kubeconform when LabRole can't run webhooks). Boundary: background:false (admission-time only, no audit of pre-existing pods); single-node local kind, Academy LabRole live-enforcement not probed."}
      - {id: CN-EXTENSION-SHOWCASE, target: "Selected advanced cloud-native extensions each have a product hook, real success and failure evidence, kill switch, resource boundary and environment fallback.", status: PASS, evidence: [evidence/w3/CN-POLICY-AS-CODE-001/, evidence/w3/CN-PROGRESSIVE-DELIVERY-001/], remaining: "Reconciled 2026-07-31: the two selected extensions, Kyverno admission policy and Argo Rollouts canary, each have a product hook, real success and failure/rejection evidence, a kill switch, resource boundary and Academy fallback. Tier B/C candidates remain optional gallery choices and are not prerequisites for this acceptance target."}
      - id: SG-PRODUCT
        target: "English-first Singapore timezone experience and current local safety resources work completely."
        status: IN_PROGRESS
        evidence:
          - docs/competition/SINGAPORE-SAFETY-RESOURCE-VERIFICATION-2026-07-31.md
          - src/main/java/com/innercosmos/service/impl/SafetyServiceImpl.java
          - src/test/java/com/innercosmos/controller/SafetyControllerTest.java
          - src/test/java/com/innercosmos/service/SafetyServiceTest.java
          - web/src/components/SafetyResourceList.tsx
          - web/src/components/SafetyResourceList.test.tsx
          - web/src/i18n.test.ts
          - web/src/i18n-completeness.test.ts
        remaining: >-
          2026-07-31 machine update: the Singapore resource catalog was reverified against current
          MOH, gov.sg, SCDF and SOS primary sources; national mindline 1771 and WhatsApp 6669 1771
          are present, legacy or confusable numbers are regression-excluded, and WhatsApp resources
          now use channel-correct wa.me links. A fresh isolated-demo browser run logged in, switched
          to English, rendered the en-SG Safety Harbor with all six exact contact links and the
          product boundary, and recorded zero application-console errors or warnings. Existing
          automated coverage checks English copy, Asia/Singapore time context and locale selection.
          Keep IN_PROGRESS until a human non-author reviewer completes the full English five-space
          browser journey and signs off language quality.
      - {id: SG-RELEASE, target: "Current PDPA, cross-border LLM, store, HSA-boundary, corporate, tax, and payment checks have owner sign-off.", status: UNASSESSED, evidence: []}

  - id: G9
    name: Complete product acceptance
    status: IN_PROGRESS
    required: true
    acceptance:
      - {id: FINAL-E2E, target: "Web, PWA, and mobile complete-product journeys pass against a production-like environment.", status: IN_PROGRESS, evidence: [evidence/g9/FINAL-E2E-001/, evidence/innovation/INNO-MOBILE-007/, evidence/innovation/INNO-MOBILE-009/, evidence/demo/PUBLIC-DEMO-001/, evidence/demo/PUBLIC-DEMO-ANDROID-001/], remaining: "2026-07-24 public-Demo update: the real-provider classroom path is now machine-proven over a public HTTPS tunnel with PostgreSQL/pgvector + Redis. Two fresh users completed registration, discovery, friendship, private group membership, Aurora -> memory settlement, capsule publish/discovery/chat and slow-letter send; the exact public APK was installed on Android API 36.1, registered a fresh account inside the shell and received a real Aurora multi-bubble response with zero crash/ANR. Still IN_PROGRESS rather than inflated to PASS: a physical Android sideload/cellular smoke, the standing PKCE-native-shell release path, the one documented shared-seed Playwright flake, and non-author acceptance remain."}
      - {id: FINAL-DEMO, target: "An 8-12 minute reproducible demo proves product differentiation, AI depth, data lineage, and Kubernetes operations.", status: IN_PROGRESS, evidence: [evidence/g9/FINAL-DEMO-001/, evidence/g9/TRACEABILITY-SNAPSHOT-2026-07-17.md, evidence/integration/FINAL-P0-CLOSURE-2026-07-22/, evidence/demo/PUBLIC-DEMO-001/, evidence/demo/PUBLIC-DEMO-ANDROID-001/], remaining: "2026-07-24: scripts/demo/run-public-demo.ps1 is now the classroom authority. One command creates the public HTTPS tunnel, builds an origin-bound Android APK, starts PostgreSQL/pgvector + Redis + real DeepSeek/Qwen services, and self-verifies the full core trajectory including social relations. Android emulator install/in-App registration/Aurora and public-vs-installed APK hash equality are proven. Remaining for PASS: one physical-phone/cellular rehearsal and a non-author delivery of the documented 8-12 minute narrative; the Kubernetes showcase remains a separate presentation segment, not a runtime dependency for this laptop-server Demo."}
      - {id: FINAL-USABILITY, target: "Non-author participants complete core tasks and articulate the unique value without developer explanation.", status: UNASSESSED, evidence: []}
      - {id: FINAL-OPERATIONS, target: "Release, rollback, incident, provider failure, data-rights, and disaster-recovery runbooks are independently exercised.", status: IN_PROGRESS, evidence: [docs/operations/README.md, docs/operations/operations-contract.yml, evidence/g9/FINAL-OPERATIONS-001/summary.md], remaining: "Reconciled 2026-08-01: the owner-authorized signed OCI release path is now machine-executed and failure-inclusive. v0.1.0-rc.1 is a non-draft Pre-release at commit 188b708a with SBOM, max-mode provenance and verified keyless Cosign signature; the prior CVE-2026-41695 gate failure is retained in evidence. The six-scenario contract now resolves 16 current artifacts and 46 source assertions. Remaining: independent receipt sign-off plus release-to-rollback rehearsal, incident tabletop plus controlled incident, production-faithful no-fallback Provider failure/recovery, disposable-account data-rights rehearsal, and encrypted off-cluster backup restore."}
      - {id: FINAL-TRACEABILITY, target: "Every required acceptance item points to current reproducible evidence and no hidden P0 blocker remains.", status: PASS, evidence: [evidence/g9/FINAL-TRACEABILITY-001/, docs/goal/complete-product-acceptance.yml, docs/audit/2026-07-23-gemini-master-audit-reconciliation.md, docs/competition/VERIFICATION-SNAPSHOT-2026-08-01.md], remaining: "Reconciled 2026-08-01 at verified release checkpoint 188b708a: every repository-path evidence reference resolves; main CI and release runs are linked; the dependency-gate failure and repair, signed digest, remote-branch cleanup, retained mentor branch, deferred history rewrite and still-unapplied main protection are explicit. Non-PASS items remain visible and include named external/human gates plus machine/environment work; this traceability PASS does not promote those items or the overall goal to a terminal state."}

completion_rule:
  required_gate_status: PASS
  required_human_gate_status: PASS
  allow_release_candidate_blocked_by_human_gate: true
  complete_when: Every required gate and acceptance item is PASS with current evidence and required independent review.
  release_candidate_blocked_by_human_gate_when: >-
    Every machine-executable required item is PASS and every remaining non-PASS item is mapped to a
    named external credential, device, legal, qualified-expert, real-user or irreversible-account gate
    with owner, required input and closure procedure. Convenience, uncertainty, missing tests, missing
    implementation, unavailable local setup or agent self-review never qualify as a human gate.
