# Final complete-product Goal Mode prompt

在仓库根目录启动长期 Goal Mode，完整阅读 `AGENTS.md`、`goal-objective.md`、`对齐文档/README.md`、`对齐文档/24-完全体最终收敛与云原生课程战役.md`、`对齐文档/25-云原生高级能力展柜与课程评分设计.md`、`docs/audit/2026-07-23-gemini-master-audit-reconciliation.md`、`docs/goal/closure-campaign-state.yml`、`docs/goal/complete-product-acceptance.yml`；涉及运行/Academy 时再完整阅读文档 14、18。以 24 为唯一当前执行权威，持续推进到 `COMPLETE`，或只剩真正外部授权/设备/法务/专业签字时达到 `RELEASE_CANDIDATE_BLOCKED_BY_HUMAN_GATE`。不要因一次提交、一个 phase 或子 Agent 返回而结束。

先执行 W0：只集成 `42d6253`、`d6925f8`、`1fc014b`、`6867c0f` 四个锁定来源；禁止通配选择旧 worktree 分支，丢弃各分支 generated bundle，在集成树语义合并 source/test/lockfile 后只构建一次，并独立跑集成门、对账 HEAD 与机器状态。保留当前所有无关/用户脏改。

W0 后立即执行 W0V：逐项复现或反证 Gemini 审查线索，按独立复核合同关闭 28 confirmed、6 partial、1 duplicate、1 false。不要照抄错误修法：不得重复创建已有前导索引，不得复用 V21，不得用 `@Order` 排序异步完成，不得只动态拼 vector 维度，不得用关键词删除代替 prompt/data 隔离，也不得把当前完整回复后 SSE 切片误当作 provider token 已先泄漏。P0 数据、隐私、安全和旧 turn 竞态关闭前，W1/W2/W3 不得标记完成。

随后按 24 并行推进 Living Intelligence/完整体验与 Cloud-Native Showcase。最多 2—3 个并发 Agent；主 Agent 唯一维护共享 bundle、迁移序列、locale/API registry、全局 ledger/state 与最终合并。优先把 Aurora、记忆画像、共鸣体、匹配、慢信六条黄金闭环做成真实可运行、可恢复、可评测的体验；同时完成三条 Kubernetes 英雄链路：SSE Pod 故障无损恢复、outbox backlog 驱动 worker 弹性、隐私安全 OTel 异步语义 trace。先修 probe/metrics/NetworkPolicy/backup/evidence/schema 可信度问题。Tier A 完成后，按 24 的评分规则尽可能实现 Argo CD、cert-manager/External Secrets、Falco、Chaos Mesh、Velero、OpenCost、OpenFeature、SPIFFE/mesh、Indexed Jobs，以及有独立正向收益的 Dapr/Knative/Crossplane/GraphQL BFF 等扩展；技术复杂度不是否决理由，但每项都必须有 product hook、真运行、失败/拒绝证据、关闭开关与环境 fallback，不能只装 chart 或画架构图。

自主运行网站、真实浏览器、Android 模拟器、Windows Tauri、kind 和可用的 Academy 环境；创建临时账号、点击全旅程、截图/录屏、故障注入、自评并修复机器可发现的问题。采用 focused→journey→阶段/合并全门的测试节奏。每个 checkpoint 小步提交并记录 implementation/test/runtime/quality/boundary/commit；不得记录密钥、P0 正文或敏感云标识，不得夸大 Mock、manifest、单机或 Academy 证据。发现计划不优时可改实现，但必须守住最终效果、五空间 IA、隐私所有权、可逆迁移、证据真实性与云原生业务因果。

在全部机器任务关闭前持续选择 `closure-campaign-state.yml` 中最高价值的未完成项，不向用户询问可由代码阅读、测试、模拟、浏览器/设备操作或官方文档解决的问题。最终交付清洁可运行仓库、Web/Android/Windows 完整体验、kind/Academy 云原生证据、验收账本、演示与运维手册，并精确列出仅剩的人工门。
