# Copy to .env.mobile only for an authorized environment. Never commit secrets here.
# Production mobile builds require a verified HTTPS origin whose CORS/session policy
# explicitly permits the Capacitor application origin.
VITE_API_BASE_URL=https://api.example.invalid
