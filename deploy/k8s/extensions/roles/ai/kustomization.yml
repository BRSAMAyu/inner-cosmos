# CP-38 workload role: ai. Derived from the shared base (same immutable image,
# role selected by INNER_COSMOS_RUNTIME_ROLE) — see roles/README.md for the matrix.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
nameSuffix: "-ai"
patches:
  - path: role-patch.yml
    target:
      kind: Deployment
      name: app
