你现在接管 D:\code\inner cosmos，进入长期 Goal Mode。你的任务不是再写计划，而是持续实现、验证、修复并交付 Inner Cosmos 完全体。除真正的外部账户、设备、法务、专业签字或不可逆操作外，不要向用户询问可通过代码、测试、浏览器、模拟器、Docker、kind 或文档自行解决的问题；不要因为一个 phase、checkpoint、commit、子 Agent 返回或测试暂时变绿而结束。

一、接管与权威来源

开始前完整阅读，不能只浏览摘要：

1. AGENTS.md
2. goal-objective.md
3. 对齐文档/README.md
4. 对齐文档/24-完全体最终收敛与云原生课程战役.md
5. 对齐文档/25-云原生高级能力展柜与课程评分设计.md
6. docs/audit/2026-07-23-gemini-master-audit-reconciliation.md
7. docs/goal/remaining-work-handoff.md
8. docs/goal/closure-campaign-state.yml
9. docs/goal/complete-product-acceptance.yml
10. docs/goal/prompts/final-complete-product-goal-prompt.md
11. 涉及启动、移动端或 Academy 时完整阅读对齐文档 14、18。

当前关键文档提交为：

6bc56bb docs: reconcile Gemini audit findings

先确认当前 HEAD 包含该提交。运行并记录：

- git status --short --branch
- git log --oneline --decorate -15
- git worktree list --porcelain
- 当前 Java、Node、Docker、Android、Rust、kind/kubectl 环境
- 当前未提交文件

必须保留用户已有的 src/main/resources/application.yml 修改和 .agents/ 临时资产，不得覆盖、删除或误提交。禁止 reset --hard、通配 cherry-pick、历史重写和秘密泄露。使用独立 codex/ 分支承载实现。

二、先完成 W0 和 W0V

若 W0 四分支尚未集成，严格按 closure-campaign-state.yml 中锁定的四个 commit 集成。不得选择名字相似的旧 WIP 分支；只合并 source/test/lockfile，丢弃各分支 generated bundle，最后由集成树统一构建一次。

W0 后立即执行 W0V。逐项处理：

docs/audit/2026-07-23-gemini-master-audit-reconciliation.md

该合同包含：

- 28 项 confirmed
- 6 项 partial
- 1 项 duplicate
- 1 项 false

必须为每一项生成关闭 receipt：复现测试、实现、focused 验证、运行证据、commit，或者可重复的反证。不要机械照抄 Gemini 原报告。

明确禁止以下错误修法：

- 不得为 2.3 创建重复外键索引；V10/V18 已有对应首列复合唯一索引。
- 不得复用已被移动推送占用的 Flyway V21。
- 不得用 @Order 假装两个 @Async 任务已形成完成顺序。
- 不得只动态拼接 vector.length 而忽略 PostgreSQL 固定维度。
- 不得使用关键词删除式 sanitize 代替 prompt/data 隔离。
- 不得把当前“完整响应生成后再切片 SSE”误判为 provider token 已经直接泄露。

W0V P0 优先顺序：

1. 慢信 scheduler 使用 expected-state 条件更新，绝不能覆盖 block、read 或其它用户状态。
2. 记忆 importance 与后台 gravity 更新不得互相丢失；用户编辑优先。
3. PersonaChat 的 Provider RPC 必须移出数据库事务，形成 reserve → provider → finalize 的可恢复状态机。
4. Capsule P1→P2 compiler 在 Provider 调用和持久化前执行目的绑定授权、最小化、PII/secret 清除、撤回传播。
5. PromptBuilder、Persona、MemoryExtract 建立真正的 role/data boundary、最小权限 evidence 和 output leakage gate。
6. crisis/abuse 输入先做安全 Unicode normalization，并增加多轮风险状态。
7. Aurora recovery 必须 turn-scoped、可取消；旧 turn 不得清除或污染新 turn。
8. clean SSE EOF、401/403、offline、后台、logout 必须有有界恢复和停止机制。
9. 修复慢信重复 draft、空 thread 无限 loading、social double-click、账户表单提前清空、录音卸载后更新、ErrorBoundary 和长文本溢出。

每个 P0 先写能失败的 regression/concurrency/adversarial test，再实现修复。并发问题必须使用 barrier/latch/Testcontainers 或真实故障注入证明，而不是只跑普通单测。

三、完成 W1—W4 完全体

W0V 关闭后，继续推进，不要停在安全修复阶段。

W1：Living Intelligence

真正完善并验证：

- Aurora 多气泡、思考、停顿、补充、打断、停止、重规划、时间感知、WakeIntent、Self/Constitution/Emergence、关系和人格演化。
- 快慢双核、planner、speaker、critic、context assembler、skills、状态机、回放和可观测性。
- 长短期记忆、画像、来源、置信度、冲突、衰减、纠正、撤回及下游传播。
- 共鸣体授权 compiler、trait/style/habit model、动态 prompt program、speaker/critic、sandbox、版本、撤回和 simulator eval。
- 画像/记忆向量的相似、互补、成长边缘、惊喜和情境匹配策略。
- 共鸣体对话、双向同意、慢信、真人连接、拒绝、屏蔽、举报和反馈闭环。
- 心理 Skill registry、schema、版本、证据来源、同意、风险和非诊断边界。

所有 AI 优势都必须通过固定数据集、单核基线、自动评测、真实 Provider（有凭据时）、失败样本、延迟、成本、隐私和盲评准备证明。不能以“代码存在”宣称效果完成。

W2：UIUX 和多端

把 Web/PWA、Android Capacitor、Windows Tauri 做成同一完整产品，不做功能缩水的移动 Demo。

逐组件验证：

empty / loading / streaming / partial / offline / error / retry /
permission / expired / long-copy

并覆盖：

- 中文与 en-SG
- 320/390px、桌面、投影
- 200% 字体
- 键盘、触控、TalkBack/语义
- reduced-motion
- 快速切换、重复点击、杀进程、恢复网络、失效认证
- visual regression、axe、性能预算

Android 模拟器和 Windows Tauri 必须实际启动，并完成 PKCE、Aurora、恢复、通知、深链、语音、离线草稿、安全存储和退出清理。Apple 签名/APNs/真机属于外部门禁，但代码、Xcode workspace、配置模板和 Mac runbook 必须完成。

W3：Kubernetes 与云原生课程证明

优先完成三条英雄链路：

1. 删除正在输出 Aurora SSE 的 API Pod，使用 durable timeline + Last-Event-ID 无重复、无丢失恢复。
2. outbox backlog/oldest-age 驱动 KEDA worker 扩缩，证明 lease competition、inbox idempotency、scale-in 和 worker 故障恢复。
3. OTel/Tempo 或 Jaeger 贯穿 API/SSE → Provider → retrieval → outbox → worker → memory/profile → WakeIntent，且不记录正文、密钥或稳定用户标识。

随后完成文档 24/25 的 Foundation 和高价值扩展：

- role-specific startup/readiness/liveness
- HPA/PDB/Gateway API/NetworkPolicy
- Argo CD、Argo Rollouts/Flagger
- Kyverno/Cosign
- cert-manager/External Secrets
- Falco
- Chaos Mesh/Litmus
- Velero
- OpenCost
- OpenFeature
- Cilium/Hubble
- SPIFFE/mesh
- Indexed Jobs
- 有独立产品收益时的 Dapr、Knative、Crossplane、GraphQL BFF

每项必须包含 product hook、真实运行、成功证据、失败/拒绝证据、kill switch、资源边界和 Academy fallback。仅安装 chart、提交 YAML 或画图不能记为完成。完整插件实验优先在本地 kind；Academy 每次重新探测能力，不夸大受限环境；commercial-sg 保留生产架构合同。

W4：最终收敛

完成：

- 全栈 clean-room 启动
- Java/Web/Vitest/Playwright/i18n/secret/IaC/image/supply-chain 全门
- Web/PWA/Android/Windows 黄金旅程
- kind 云原生故障演练
- 可用时的 Academy rehearsal
- SBOM、镜像 hash、APK/MSI hash
- 需求→实现→测试→证据矩阵
- 演示脚本、运行手册、故障恢复和回滚手册
- acceptance ledger 与 closure state 的真实对账

四、协作规则

最多使用 2—3 个并发 Agent：

- Integrator：独占 ledger/state、Flyway 序列、共享 API/types/locale、generated bundle、合并和最终验证。
- Backend/AI Agent：状态机、事务、数据边界、AI runtime。
- Frontend/Cloud Agent：一次只承担一个边界清晰且文件冲突低的纵向任务。

子 Agent 必须从锁定 commit 建独立分支/worktree，返回：

- commit
- diff scope
- focused tests
- runtime evidence
- 已知边界
- 待集成事项

主 Agent 必须独立读 diff 和复跑高风险测试，不能仅相信子 Agent 的 green 报告。

五、测试与 Git 节奏

- 编辑循环：lint/type/focused test。
- 纵向 checkpoint：模块测试 + 一条真实旅程。
- 并发、安全、迁移、Redis/PostgreSQL、SSE、Kubernetes 生命周期：专项故障/集成测试。
- 合并和 release candidate：全项目测试。

每个可回滚 outcome 单独提交。不得为了追求测试数量写无断言、只检查 200、只验证元素存在或复制实现逻辑的测试。

六、唯一合法终态

只有以下两种终态：

1. COMPLETE：
   所有机器可执行任务完成，acceptance ledger 必需项有当前可复现证据。

2. RELEASE_CANDIDATE_BLOCKED_BY_HUMAN_GATE：
   剩余项目只能是明确的真实外部 Provider/云账户/Apple/商店/法务/心理专家/真实用户研究门禁。

缺代码、缺测试、未启动环境、Agent 未尝试、需要视觉检查、需要模拟器点击、需要浏览器测试，都不是人类门禁。

最终创建干净提交，推送工作分支并创建 PR。最终报告必须区分：

- 已实现且验证
- 部分实现
- 未验证
- 环境阻塞
- 外部人类门禁

在达到上述合法终态之前持续选择最高价值未完成项推进，不要中断并等待用户逐步指挥。