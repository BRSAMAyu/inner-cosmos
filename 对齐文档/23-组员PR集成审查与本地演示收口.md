# 23 组员 PR 集成审查与本地演示收口

> 日期：2026-07-21
>
> 状态：`LOCAL_TEACHER_DEMO_READY / COMMERCIAL_RELEASE_BLOCKED_BY_HUMAN_GATES`
> 证据：`evidence/integration/PR-REVIEW-2026-07-21/README.md`、`evidence/integration/FINAL-P0-CLOSURE-2026-07-22/README.md`

## 1. 本轮集成裁决

组员 PR #5、#6、#7 已作为一个组合变更完成代码审查、运行时体验复核与本机集成。它们分别补齐真实 Provider 流式失败回退、CI/依赖/时区稳定性，以及无记忆共鸣体与 Kubernetes 工作负载加固。

审查没有机械照单全收。PR #6 的 Secret 扫描改造曾只扫描 Git 已追踪文件，遗漏尚未暂存的新文件；本轮将范围修正为“已追踪 + 未追踪但未忽略”，并用一次不含真实凭据的负向测试证明门禁有效。PR #5 声称覆盖 200 空流，但原测试只覆盖非 2xx；本轮增加了相应回归。

## 2. 面向用户的收口

- Aurora 的多气泡、可打断、停止、重规划、耐久时间线、WakeIntent 与连续自我语义均保留。
- 两条 Aurora 主动发送即使同时遇到旧 CSRF token 失效，也会共享一次刷新并分别恢复流式握手；该路径已有并发传输层回归，不再依赖“等上一条结束”的 workaround。
- 现场输入“展示既兴奋又担心”的真实旅程会产生先共情、再聚焦下一步的两气泡回应；不再只是静态页面证明。
- 共鸣体允许用户在没有选中记忆时创建“通用侧面”的严格私有预览，不虚构摘要、不自动公开，并保留后续授权入口。
- 390×844 移动视图去掉了对话区与页面的双滚动条竞争；底部五空间导航、输入区与安全边界仍可用。
- 桌面视图为吸顶导航保留自动滚动间距，避免对话目标被遮挡。

## 3. 网站、API 与 App 形态

- 网站仍以 React 19 + Vite/PWA 五空间体验为主要教师演示入口。
- OpenAPI 3.1、生成客户端与破坏性变更规则继续作为 Web、App 与外部集成的统一 API 契约；本轮未另造平行接口。
- Capacitor Android/iOS 壳继续复用同一产品体验与契约。本轮重新同步原生资源并成功生成最新 Android debug APK。
- APK 可安装产物位于 `web/android/app/build/outputs/apk/debug/app-debug.apk`。本机没有连接设备，也没有配置 AVD，因此“真实设备启动”仍是设备门禁，不能由构建成功替代。

## 4. 验收事实

- Java：923/923，0 failures/errors/skipped；PostgreSQL 16、pgvector、Flyway V1–V20、Redis Testcontainers 路径通过。
- 前端：Vitest 34 files、227/227；包含 Aurora 并发 CSRF 自动恢复；Vite/PWA production build 通过。
- Browser E2E：既有 Playwright 14/14；本轮再次以真实浏览器复核登录、Aurora 对话、共鸣体严格预览、桌面和 390×844。
- Secret：最终扫描 0 findings，并覆盖未追踪非忽略文件。
- Kubernetes：Academy 20 个资源验证通过；Trivy HIGH/CRITICAL 为 0。
- Android：Gradle `assembleDebug` 通过，245 tasks；APK 6,415,241 bytes。

## 5. 仍不能由 Agent 关闭的门禁

1. 真实 Provider 密钥吊销/轮换、非作者盲评、长期效果、延迟与成本证据。
2. 真机 Android/iOS、APNs/FCM、发布签名、商店账号与商店审核。
3. 新加坡 PDPA 跨境、心理专业边界、HSA/法务与运营复核。
4. commercial-sg 的 AWS、DNS、支付、备份恢复和非作者运维演练。

上述边界之外，本轮可机器完成的 PR 集成、网站体验收口、API 契约验证与 Android 可安装构建均已完成。后续不得把旧拆轨 WIP 再次 cherry-pick，也不得把 APK 构建夸大为真机和商店 PASS。

## 6. 2026-07-22 P0 耐久性补充检查点

在上述 PR 集成之后又关闭了四个会直接影响真实演示与多副本正确性的缺口，完整证据见 `evidence/integration/FINAL-P0-CLOSURE-2026-07-22/README.md`：

- 对话结束事件不再只写 outbox 审计记录；worker 会耐久地执行记忆抽取、画像候选与情绪时间线投影，失败时 receipt 与副作用一并回滚并重试。
- production outbox 模式不会同时注册旧的六个进程内异步 listener，避免多副本下重复派生。
- Aurora 的长期记忆上下文改为任务感知 `MemoryEvidencePack`，并让 legacy context 使用同一组 evidence IDs；检索失败不会悄悄退回与任务无关的重力 Top-N。
- Academy 的 API、worker、scheduler 启动前均精确等待仓库最高 Flyway V20，失败 migration 会 fail closed；离线清单验证为 20 resources、3/3 schema gates。
- `scripts/run-teacher-demo.ps1` 提供隔离、可清理且自检 health/CSRF/login/session/React shell 的教师演示入口。
- 当前 Java 全量回归提升为 923/923；CI 最低门槛同步提升到 923，避免今后删除测试后仍被历史 613 门槛放行。
